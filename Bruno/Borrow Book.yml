info:
  name: Borrow Book
  type: http
  seq: 11

http:
  method: POST
  url: localhost:8080/api/books/borrow/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

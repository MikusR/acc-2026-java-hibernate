info:
  name: Delete Book
  type: http
  seq: 5

http:
  method: DELETE
  url: localhost:8080/api/books/1
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

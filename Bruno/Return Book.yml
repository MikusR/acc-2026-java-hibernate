info:
  name: Return Book
  type: http
  seq: 12

http:
  method: POST
  url: localhost:8080/api/books/return/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

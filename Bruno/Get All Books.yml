info:
  name: Get All Books
  type: http
  seq: 2

http:
  method: GET
  url: localhost:8080/api/books
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

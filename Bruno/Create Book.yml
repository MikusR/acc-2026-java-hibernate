info:
  name: Create Book
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/books
  body:
    type: json
    data: |-
      {
      "title":"t",
      "author":"a",
      "genre":"g",
      "publicationYear":1999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

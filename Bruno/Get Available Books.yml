info:
  name: Get Available Books
  type: http
  seq: 7

http:
  method: GET
  url: localhost:8080/api/books/available
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Update Book
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/api/books/1
  body:
    type: json
    data: |-
      {
      "title": "Updated Title",
      "author": "Updated Author",
      "genre": "Updated Genre",
      "publicationYear": 2005,
      "available": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

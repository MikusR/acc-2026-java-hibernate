info:
  name: Update Book
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/api/books/2
  body:
    type: json
    data: |-
      {
      "title": "Updated Title2",
      "author": "Updated Author2",
      "genre": "ds",
      "publicationYear": 4005,
      "available"=true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

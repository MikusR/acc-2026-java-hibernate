info:
  name: Get Book By Id
  type: http
  seq: 3

http:
  method: GET
  url: localhost:8080/api/books/1
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

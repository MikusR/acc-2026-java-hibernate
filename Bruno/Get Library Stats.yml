info:
  name: Get Library Stats
  type: http
  seq: 13

http:
  method: GET
  url: localhost:8080/api/books/stats
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
